packages:
  # include packages in sub-folders (e.g. apps/ and packages/)
  - 'apps/**'
  - 'packages/**'
  # if required, exclude directories
  - '!**/test/**'
allowBuilds:
  sharp: true
  unrs-resolver: true
